create TABLE person(
    id SERIAL PRIMARY KEY,
    firstName VARCHAR (255),
    surName VARCHAR(255)
)

create TABLE person(
    id SERIAL PRIMARY KEY,
    title VARCHAR (255),
    content VARCHAR(255)
)